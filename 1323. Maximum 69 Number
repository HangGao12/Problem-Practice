    def maximum69Number (self, num):
        """
        :type num: int
        :rtype: int
        """
        num_str = list(str(num))
        for x in range(0, len(num_str)):
            if num_str[x] != '9':
                num_str[x] = '9'
                break
        return int(''.join(num_str))
